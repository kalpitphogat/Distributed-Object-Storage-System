// MultipartService is registered inside ObjectsModule (not here) to avoid a
// circular module dependency between ObjectsModule ↔ MultipartModule.
// This file exists as a placeholder for Phase 5, when the multipart endpoints
// may be refactored into a standalone module with forwardRef().
export {};
